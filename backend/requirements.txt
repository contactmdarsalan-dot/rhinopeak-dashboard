# RhinoPeak Backend — production pinned dependencies
# Install with: pip install -r requirements.txt

# Django with MongoDB support
Django>=4.2,<5.0
django-mongodb-backend>=2.2

# Database driver
pymongo>=4.6,<5.0

# Auth & tokens
PyJWT>=2.8,<3.0
cryptography>=41.0,<43.0

# HTTP client (used by payment services)
urllib3>=2.0,<3.0

# Redis for rate limiting (optional — app falls back to in-memory if unavailable)
redis>=5.0,<6.0

# Server
gunicorn>=21.0,<23.0

# Environment variables
python-dotenv>=1.0,<2.0

# Email (Brevo SMTP built-in; no extra deps needed beyond stdlib smtplib)

# SMS (Twilio — optional, falls back to logging if not configured)
twilio>=8.0,<9.0

# ── Dev / testing ──────────────────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-django>=4.8,<5.0
pytest-asyncio>=0.23,<1.0